[project]
name = "memory-proxy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.123.0",
    "httpx>=0.28.1",
    "ipadic>=1.0.0",
    "mecab-python3>=1.0.12",
    "ruff>=0.14.7",
]
